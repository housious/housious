{{ 'featured-collection.css' | asset_url | stylesheet_tag }}

{% assign selected_collection = section.settings.collection %}

{% if selected_collection != blank %}

{% comment %} <section class="featured-collection-products section-container {{ section.settings.container_width }}"> {% endcomment %}

<section class="featured-collection-products section-container {{ section.settings.container_width }}" style="--padding-top: {{ section.settings.padding_top_desktop }}px; --padding-bottom: {{ section.settings.padding_bottom_desktop }}px; --padding-top-mobile: {{ section.settings.padding_top_mobile }}px; --padding-bottom-mobile: {{ section.settings.padding_bottom_mobile }}px;">
  <div class="page-width">

    {% if section.settings.heading != blank %}
      <div class="section-header">
        <h2>{{ section.settings.heading }}</h2>

        <a href="{{ selected_collection.url }}" class="view-all-link">
          View All
        </a>
      </div>
    {% endif %}

    <div class="products-grid">

      {% for product in selected_collection.products limit: section.settings.products_to_show %}

        {% render 'product-card',
          card_product: product
        %}

      {% endfor %}

    </div>

  </div>
</section>

{% endif %}

{% comment %} {% schema %}
{
  "name": "Featured Collection",
  "settings": [
    {
      "type": "select",
      "id": "container_width",
      "label": "Container Width",
      "options": [
        {
          "value": "section-container--lg",
          "label": "Large"
        },
        {
          "value": "section-container--xl",
          "label": "Extra Large"
        },
        {
          "value": "section-container--full",
          "label": "Full Width"
        }
      ],
      "default": "section-container--xl"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Best Sellers"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Products to show",
      "min": 4,
      "max": 20,
      "step": 1,
      "default": 5
    }
  ],
  "presets": [
    {
      "name": "Featured Collection Products"
    }
  ]
}

{% endschema %} {% endcomment %}

{% schema %}
{
  "name": "Featured Collection",
  "settings": [
    {
      "type": "select",
      "id": "container_width",
      "label": "Container Width",
      "options": [
        {
          "value": "section-container--lg",
          "label": "Large"
        },
        {
          "value": "section-container--xl",
          "label": "Extra Large"
        },
        {
          "value": "section-container--full",
          "label": "Full Width"
        }
      ],
      "default": "section-container--xl"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Best Sellers"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "label": "Products to show",
      "min": 4,
      "max": 20,
      "step": 1,
      "default": 5
    },
    {
      "type": "header",
      "content": "Section Spacing"
    },
    {
      "type": "range",
      "id": "padding_top_desktop",
      "label": "Top Spacing (Desktop)",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom_desktop",
      "label": "Bottom Spacing (Desktop)",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Top Spacing (Mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Bottom Spacing (Mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Featured Collection Products"
    }
  ]
}
{% endschema %}